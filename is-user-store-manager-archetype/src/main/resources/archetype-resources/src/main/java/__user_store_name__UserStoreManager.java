package ${package};

public class ${user_store_name}UserStoreManager extends AbstractUserStoreManager {

    private static final Log log = LogFactory.getLog(${user_store_name}UserStoreManager.class);

}